# NPM Workspace Configuration
# Enable workspaces
workspaces=true

# Install dependencies in workspace root
install-strategy=hoisted

# Enable package-lock for workspaces
package-lock=true

# Save exact versions for dependencies
save-exact=true
